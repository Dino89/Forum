


<div id="content">qqqqq
<p id="notice"><%= notice %></p>


<p>
  <b>Text:</b>
  <%= @comment.text %>
</p>

<p>
  <b>Title:</b>
  <%= @comment.title %>
</p>

<p>
  <b>Procontra:</b>
  <%= @comment.procontra %>
</p>

<% if current_user.present? %>
	<% if current_user.has_role? :admin  %>
		<p>
	  		<b>Id:</b>
	  			<%= @comment.id %>
		</p>
		<p>
	 		<b>Theme:</b>
	  			<%= @comment.theme_id %>
		</p>
		<p>
	  		<b>User:</b>
	  			<%= @comment.user_id %>
		</p>
	<% end %>	
<% end %>


<%= link_to 'Edit', :controller => "comments", :action => "edit", :theme => @comment.theme_id %> |

<%= link_to 'Back', :back %>
</div>
