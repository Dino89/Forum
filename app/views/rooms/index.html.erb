<h1>Listing rooms</h1>

<div id="content">
        <div id="catlist">
            <ul>
			   <% @rooms.each do |room| %>



				<li> <%= link_to room.title, room %> <%= link_to 'Edit', edit_room_path(room) %> <%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></li>
                        


					 <% end %>

            </ul>
        </div>
</div>


<br />

<%= link_to 'New Room', new_room_path %>
